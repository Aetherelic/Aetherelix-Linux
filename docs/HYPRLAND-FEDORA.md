# Hyprland on Fedora

Fedora support is handled as a project-specific packaging choice.

Early development uses this COPR:

sudo dnf copr enable ashbuk/Hyprland-Fedora

This may change later if Hyprland lands cleanly in Fedora official repositories or if the project maintains its own packages.
